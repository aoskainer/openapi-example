user:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: integer
      format: int64
    name:
      type: string

users:
  type: array
  maxItems: 100
  items:
    $ref: "#/user"
